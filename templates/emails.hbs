<h1>Pretend-that-I-have-friends App</h1>

<ul>
  {{#each email in model}}
    <li>{{#link-to 'email' email.id}}{{email.subject}}{{/link-to}}</li>
    <ul>
      <li><strong>Recipient:</strong> {{email.address}} </li>
    </ul>
  {{/each}}
</ul>

{{#link-to 'new-email' classNames="btn btn-primary"}}Compose new Email{{/link-to}}
